Next steps is making everything into a script that needs toi be a script, the stuff in notebooks and the hmm.txt


Run every seq through seq2topt if it runs quickly despite obvious limitations. It is useful to have

After this is done. Must extract the sequences with the highest scores from scoring system. Remember to ignore the bad HMMS
scoring system should be the full sequence hmm, 2 PF00154 hmms and then all string matches

# Create plots summarising the top_hits